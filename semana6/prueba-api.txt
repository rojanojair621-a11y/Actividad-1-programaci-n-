l enviar una petición POST a la ruta /registro con un nombre y un mensaje en formato JSON, el servidor recibe los datos mediante req.body y responde con un objeto JSON que confirma la recepción de la información. La respuesta muestra el estado, el nombre y el mensaje enviados por el usuario.

